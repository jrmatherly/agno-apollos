#!/usr/bin/env bash
#MISE description="Install all dependencies (creates .venv)"
#MISE alias="install"
set -e

echo "Installing dependencies..."
uv sync --dev

echo ""
echo "Done. Activate with: source .venv/bin/activate"
echo "Or run commands with: uv run <command>"
