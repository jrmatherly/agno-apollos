#!/usr/bin/env bash
#MISE description="Run all checks (backend + frontend)"
#MISE depends=["lint", "typecheck", "frontend:validate"]
set -e

echo "Checking backend formatting..."
uv run ruff format --check .
uv run ruff check --select I .
echo ""
echo "All checks passed."
